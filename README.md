A little fun project to remind myself on how API's work. 

This app uses the `requests` library in python to feth data from `https://api.kanye.rest/` and then display this on a tkinter application window.
