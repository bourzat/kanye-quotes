A little fun project to remind myself on how API's work. 

This app uses the `requests` library in python to feth data from `https://api.kanye.rest/` and then displays this on a tkinter application window. It's an API with a bunch of random Kanye quotes. 
